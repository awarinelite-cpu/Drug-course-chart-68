{
  "name": "drug-course-chart-68",
  "version": "1.0.0",
  "description": "68 Nigerian Army Reference Hospital Yaba ward charting app (vanilla JS/Firebase), wrapped as an Android app via Capacitor.",
  "private": true,
  "scripts": {
    "build:www": "node scripts/build-www.js",
    "cap:sync": "npm run build:www && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/push-notifications": "^8.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
